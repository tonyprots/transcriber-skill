---
name: transcriber
description: >-
  Локальная расшифровка аудио и видео: дословный и читаемый текст, субтитры,
  разделение по спикерам, очередь спорных мест, словарь терминов. Используй,
  когда просят транскрипт, субтитры, протокол записи, распознать голосовое или
  разделить разговор по голосам. Русский и английский идут проверенными
  маршрутами, остальные языки — с предупреждением. Ничего не отправляет во
  внешние сервисы и не меняет исходный файл.
license: MIT
compatibility: >-
  Python 3.10+, ffmpeg. Полный набор (Whisper Turbo MLX, диаризация) — macOS
  Apple Silicon; на Linux и Intel Whisper работает на CPU, диаризация недоступна.
metadata:
  author: Anton Protsenko (tonyprots.ru)
  version: "0.10.0"
  homepage: https://github.com/tonyprots/transcriber-skill
---

# Локальная расшифровка аудио

Делай расшифровку воспроизводимо: сохраняй обе исходные гипотезы, отдельно
читаемый результат, список автоматических исправлений и очередь спорных мест.
Не выдавай отредактированный текст за дословный.

Почему конвейер устроен так — [references/decisions.md](references/decisions.md).
Туда же отсылай пользователя, когда он спрашивает «почему так долго» или
«почему скилл не исправил сам».

## Подготовить окружение

Найди каталог скилла и питон из его окружения. Системный `python3` не годится:
в нём нет `mlx_audio`, проверочная модель тихо отвалится, и `max` выродится в
`fast`. Скилл может быть подключён симлинком, поэтому путь резолвится, а `.venv`
ищется и рядом со скиллом, и на два уровня выше — в корне репозитория:

```bash
for base in ".claude/skills/transcriber" "$HOME/.claude/skills/transcriber" \
            "$HOME/.codex/skills/transcriber" \
            "$HOME/.local/share/transcriber-skill/skills/transcriber"; do
  [ -f "$base/SKILL.md" ] || continue
  SKILL_DIR="$(python3 -c 'import os,sys; print(os.path.realpath(sys.argv[1]))' "$base")"
  break
done
for candidate in "$SKILL_DIR/.venv" "$SKILL_DIR/../../.venv"; do
  [ -x "$candidate/bin/python" ] && ASR_PYTHON="$candidate/bin/python" && break
done
echo "${SKILL_DIR:-скилл не найден} / ${ASR_PYTHON:-окружения нет}"
```

Если скилл лежит в другом месте (пользователь клонировал репозиторий к себе),
подставь его путь первым в список — остальное не меняется.

Нет `ASR_PYTHON` — окружения ещё нет. Предупреди, что установка займёт
несколько минут, около 1 ГБ на пакеты и 2,7 ГБ на модели, и после согласия
пользователя выполни:

```bash
bash "$SKILL_DIR/scripts/setup.sh" --models ru   # .venv, зависимости и модели русского маршрута
```

`--models en` или `--models all` берут модели для английского, `--diarize`
добавляет Sortformer. Без `--models` модели подтянутся при первом запуске.
Размер любого маршрута считает сам скилл:
`"$ASR_PYTHON" "$SKILL_DIR/scripts/prefetch_models.py" ru --print-size`.

Установка заканчивается проверкой `scripts/doctor.py`: Python, ffmpeg, пакеты,
какие модели в кэше, их ревизии и даты замеров. Его же запускай, когда что-то не
работает.

Модели лежат в `~/.cache/huggingface`. Если Hub недоступен, а модели скачаны,
конвейер сам переходит в офлайн; `--offline` делает это принудительно.
Whisper Turbo через MLX и диаризация работают только на macOS Apple Silicon: на
Linux и Intel Whisper идёт через faster-whisper на CPU, заметно медленнее, а
`--diarize` отказывает сразу с понятным текстом.

## Заметить, что модели устарели

Скилл не обновляет модели сам: маршрут держится на замерах WER, а не на номере
версии. Но `doctor.py` печатает дату последнего замера рядом с каждой моделью и
предупреждает, если замеру больше полугода или установленная `onnx-asr` знает
версию новее. С сетью он проверяет ещё и хаб:

```bash
"$ASR_PYTHON" "$SKILL_DIR/scripts/doctor.py" --check-updates
```

Увидел предупреждение — скажи пользователю: расшифровку это не блокирует, но
решение принимает он. Объясни, что известно (вышла версия новее, локальных
замеров на ней нет) и чего это стоит: без прогона своей выборки цифры в
`references/quality.md` перестанут описывать результат. Сам модель не подменяй и
дату `calibrated` в [catalog.py](scripts/audio_transcription/catalog.py) без
нового замера не двигай.

Если модель исчезла с хаба, прогон падает с ошибкой загрузки. Не ищи замену
молча: скажи пользователю и предложи выбрать новую вместе.

## Выбрать режим

- `max` (по умолчанию) — обе модели маршрута на каждом окне. Русский: GigaAM v3
  E2E плюс проверяющий Whisper Turbo. Английский наоборот: Whisper Turbo плюс
  GigaAM Multilingual CTC int8. Для языков без калибровки остаётся один Whisper
  и явное предупреждение.
- `fast` — одна основная модель. На русском к ней добавлена лёгкая Vosk ru: она
  собирает ту же очередь расхождений за 2% длительности. На остальных языках
  `fast` идёт без очереди.

Не указали режим — бери `max`. Он отмечает 98–100% ошибок распознавания, и
переслушивать нужно только отмеченные окна. Для записей с продуктовыми
названиями и латиницей выбирай `max`, даже когда пользователь торопится:
проверяющая Vosk транслитерирует термины, и из её расхождений не собрать запись
словаря. Разбор обоих режимов — в [decisions.md](references/decisions.md).

Проверяющая модель только отмечает окна и никогда не правит текст. Расхождение
означает «здесь стоит послушать», а не «вторая модель права».

Ориентир на Apple M1 (замеры 2026-09-06 и 2026-09-12): `max` — четверть-треть
длительности записи, час аудио за 15–20 минут; `fast` — около 10% длительности
на русском и 8% на остальных языках; диаризация добавляет 10–45 секунд на
загрузку модели. Для записи длиннее часа назови эту оценку заранее. Фактические
времена стадий пишутся в `manifest.json` → `timings_seconds`, кто именно
проверял — в `language_route.verifier_used`.

## Запустить

```bash
"$ASR_PYTHON" "$SKILL_DIR/scripts/transcribe.py" INPUT \
  --mode max \
  --language ru \
  --output OUTPUT_DIR
```

`OUTPUT_DIR` скрипт создаёт сам. Не делай `mkdir` заранее и ничего в него не
клади: непустой каталог без `--overwrite` — отказ. Лог фонового запуска держи
**рядом**, не внутри. `--overwrite` без необходимости не ставь: прежний
результат уезжает в архивный каталог рядом.

Ход работы идёт в stderr (этапы и счётчик окон, не чаще раза в 20 секунд,
отключается `--quiet`), в stdout — только итоговый JSON с путём результата.
Запись длиннее пяти минут запускай в фоне и следи по логу:

```bash
"$ASR_PYTHON" "$SKILL_DIR/scripts/transcribe.py" INPUT \
  --mode max --language ru --output OUTPUT_DIR > OUTPUT_DIR.log 2>&1 &
```

Ограничивать прогон снаружи не надо — за зависанием следит встроенный watchdog,
а GNU `timeout` на macOS всё равно нет. Строка «Результат записан … Уборка
временных файлов» значит, что каталог уже готов и его можно читать: последующая
пауза — это уборка временных WAV, а не зависание.

Остальные флаги: `--glossary PATH.yaml` — свой выверенный словарь поверх того,
что скилл ведёт сам; `--no-learn` — не пополнять свой словарь этой записью;
`--no-glossary` — прогон вообще без словарей; `--whisper-backend auto` — MLX на
Apple Silicon и CPU иначе; `--no-cache` — контрольный прогон мимо кэша гипотез
(кэш живёт в `~/.cache/transcriber`); `--version` — версия скилла, та же, что
уйдёт в манифест. Полный список — `transcribe.py --help`.

### Диаризация

Для интервью, встречи или прямой просьбы разделить говорящих добавь `--diarize`.
Если число участников известно, обязательно укажи `--expected-speakers N`: до
четырёх голосов `auto` выберет Sortformer, от пяти — FluidAudio Offline
Community-1. Те же VAD-окна режутся по сменам спикера и одинаково подаются обеим
ASR; перекрывающаяся речь получает несколько меток и не транскрибируется дважды.
Для одного голосового `--diarize` не нужен.

FluidAudio включён в пакет как macOS arm64 бинарник (происхождение и контрольная
сумма — в `third-party/fluidaudio/NOTICE.md`), его CoreML-модели ставятся
отдельно. Перед первым большим совещанием, получив разрешение пользователя на
загрузку 35 МБ:

```bash
"$ASR_PYTHON" "$SKILL_DIR/scripts/setup_fluidaudio_models.py"
"$ASR_PYTHON" "$SKILL_DIR/scripts/transcribe.py" INPUT \
  --language ru --mode max --diarize --expected-speakers 6 --output OUTPUT_DIR
```

Если число найденных голосов не совпало с ожидаемым, считай все метки спикеров
требующими проверки. На других платформах маршрута для 5+ голосов нет; не
подменяй его ограниченным Sortformer.

## Работать со словарём

Словарей два, и путать их не надо. Свой скилл ведёт сам (ниже) — он и работает
по умолчанию. Словарь пользователя передаётся флагом `--glossary`, скилл его
только читает, и при совпадении терминов сильнее именно он.

Правила ниже — про записи в любом из них. Схема и примеры —
[assets/glossary.example.yaml](assets/glossary.example.yaml).

1. Вноси каноническое написание и известные варианты распознавания.
2. `auto_apply: true` — только для однозначной точной замены. Формы склонения
   заводи отдельными записями: замена литеральная.
3. Нечёткие совпадения автоматически не исправляются, они идут в
   `review-needed.md`. Похожее ищется и по звучанию, поэтому «тэгэстат»
   находит `TGStat`, а «трэц» — `Threads`, хотя общих букв у них нет. Термины
   короче четырёх букв так не ищутся: «SMM» и «SMS» на слух неразличимы.
4. Новую ошибку добавляй алиасом только после проверки по аудио или явного
   подтверждения пользователя.
5. `raw.json` не меняй: это аудит-след исходных гипотез.

### Свой словарь скилл ведёт сам

Заводить и пополнять его руками не нужно. После каждого прогона скилл снимает
кандидатов из очереди проверки и дописывает их в `~/.transcriber/glossary.yaml`
(путь меняется флагом `--glossary-store` или переменной
`TRANSCRIBER_GLOSSARY_STORE`). Ничего не настраивая, пользователь с первых же
записей получает подсветку терминов в `review-needed.md`, а дальше — и правку.

Как запись начинает править текст. Сначала она только подсвечивается. Замена
включается сама, когда термин повторился **в трёх разных записях** и при этом:
правильное написание дала проверяющая модель, вариант звучит похоже на канон
(«значит» против «GPT» не пройдёт), он не короче четырёх букв и ни разу не
встречался вне спора — слово, в котором модели согласны, это обычное слово
языка, а не ослышка. Порог заменяет человеческое подтверждение: случайная
ослышка до него не доживает, потому что приходит из одной записи.

Что с этим делать агенту:

1. Ничего не делать в обычном прогоне. Словарь пополняется молча, строка
   «Словарь: …» в stderr говорит, что изменилось.
2. Прочитать в `review-needed.md` раздел «Скилл начал заменять», если он есть, и
   назвать пользователю новые замены. Это единственный момент, когда правка
   текста меняется сама, — о нём стоит сказать вслух.
3. Отработать раздел «Правильное написание знает только человек»: там термины,
   которые не написала верно ни одна модель. Прослушай окно, спроси у
   пользователя написание и внеси запись руками — выдумывать его скилл не
   станет.
4. Ручная правка сильнее автоматики: если поменять `auto_apply` в файле, скилл
   это поле больше не трогает. Удалённая запись вернётся из следующего
   расхождения — чтобы она не возвращалась, нужен `--no-learn`.
5. Свой выверенный словарь пользователя (`--glossary`) скилл только читает и
   никогда не меняет; при совпадении терминов он сильнее выученного.

Отдельно собрать кандидатов на подпись, не трогая рабочий словарь, по-прежнему
можно:

```bash
"$ASR_PYTHON" "$SKILL_DIR/scripts/glossary_candidates.py" OUTPUT_DIR \
  --glossary СЛОВАРЬ.yaml --output candidates.yaml
```

Перепрогон с обновлённым словарём дешёвый: гипотезы берутся из кэша,
пересчитываются только словарь и очередь (секунды вместо минут).

## Проверить результат

В каталоге результата обязательны:

- `verbatim.md` — лексическая гипотеза без редакторского сглаживания;
- `readable.md` — читаемая гипотеза без филлеров и с разрешёнными словарными
  исправлениями;
- `subtitles.srt` и `subtitles.vtt` — субтитры по читаемым сегментам;
- `segments.json` — общие окна, обе гипотезы и решения сверки;
- `glossary-audit.json` — словарные исправления и предложения;
- `review-needed.md` — расхождения моделей и возможные термины;
- `raw.json` — все исходные гипотезы, исправления и предложения;
- `manifest.json` — модели и ревизии их весов, режим, источник, тайминги стадий
  и счётчики.

При `--diarize` добавляется `speakers.json`, а метки появляются в Markdown и
субтитрах.

Всегда просматривай `review-needed.md`. Для важных имён, чисел, отрицаний,
адресов, аббревиатур и продуктовых терминов прослушай отмеченный интервал.
Проверочная модель — независимый сигнал, но её вариант не становится правильным
автоматически. При диаризации часть пунктов — краевые артефакты: слово на
границе окна попало к соседу, это видно по одному отличающемуся токену в начале
или конце.

После расшифровки можно делать резюме, протокол, список решений или задачи, но
для фактических утверждений опирайся на проверенный `readable.md`, а нерешённые
фрагменты обозначай явно.

Контракт артефактов — [references/output-contract.md](references/output-contract.md),
выбор моделей и ограничения — [references/models.md](references/models.md),
калибровка порога — [references/quality.md](references/quality.md), обоснование
решений — [references/decisions.md](references/decisions.md).

Скилл сделал Антон Проценко, [tonyprots.ru](https://tonyprots.ru). Исходники,
замеры и обратная связь:
[github.com/tonyprots/transcriber-skill](https://github.com/tonyprots/transcriber-skill).
